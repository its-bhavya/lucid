services:
  - type: web
    name: lucid-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EODHD_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
